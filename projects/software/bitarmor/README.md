# BitArmor

Hardened Bitcoin vault-management policy framework.

**Version:** 0.1.0-alpha  
**License:** MIT

The browser edition handles policy metadata and encrypted manifests only. It does not generate, import, expose, or sign with private keys.

Deploy directly into `/projects/software/bitarmor/`. `logo.png` is intentionally not bundled so an existing site logo remains untouched.
