# kinkr

Adults-only compatibility workbench with shared-interest scoring, hard-limit conflict detection, self-described media tags, and AES-GCM encrypted profile export. Hard limits always override scores.

Deploy into `/projects/software/kinkr/`.
