# HydraMe

Functional browser hydration tracker using user-selected targets, 1 L bottle units, reminders, trend charts, and JSON/CSV export. It does not prescribe medical fluid intake.

Deploy directly into `/projects/software/hydrame/`.

`logo.png` is intentionally omitted.
