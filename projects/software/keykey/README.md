# KeyKey

Generates generic CSPRNG key material, SHA3-256 and RIPEMD-160 derivations, Base64/Base58 encodings, metadata, and optional AES-GCM encrypted bundles. This browser build does not create Bitcoin wallet mnemonics.

Deploy into `/projects/software/keykey/`.
