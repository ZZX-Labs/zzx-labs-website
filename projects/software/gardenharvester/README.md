# GardenHarvester

Browser planning companion for authorized internet-radio and longform-stream archiving.

Parses timestamped metadata changes, builds split segments and filenames, generates FFmpeg capture/split commands, and exports a harvest manifest. Actual recording/encoding remains native FFmpeg.

Deploy directly into `/projects/software/gardenharvester/`.

`logo.png` is intentionally omitted.
