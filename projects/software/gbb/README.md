# Geisha’s BlackBook

Private browser catalog for adult professional creators/studios, media assets, credits, and session records.

Working data stays in memory and can be explicitly exported/imported as a PBKDF2-HMAC-SHA256 + AES-256-GCM encrypted catalog. The page is for adult records only and does not replace consent/release documentation.

Deploy directly into `/projects/software/gbb/`.

`logo.png` is intentionally omitted.
