(()=>{"use strict";const $=id=>document.getElementById(id);let plan=[],timer=null,remaining=0,logs=[];const lib={warm:["brisk walk","arm circles","hip hinge drill","easy squat"],cardio:["fast walk intervals","stationary bike","step-ups","marching intervals"],core:["dead bug","bird dog","side plank","front plank","glute bridge"],strength:["bodyweight squat","incline push-up","reverse lunge","hip hinge","row variation"],mob:["hamstring stretch","hip flexor stretch","thoracic rotation","calf stretch"],breath:["box breathing","slow nasal breathing","4-6 breathing"]};const pick=(a,n)=>[...a].sort(()=>Math.random()-.5).slice(0,n);$("build").onclick=()=>{const minutes=Math.max(30,Math.min(120,+$("minutes").value||45)),diff=$("difficulty").value,rec=$("recovery").value,scale=rec==="poor"?.75:rec==="great"?1.1:1,w=Math.round(minutes*scale);plan=[{name:"Breath + warm-up",minutes:Math.max(5,Math.round(w*.12)),items:[...pick(lib.breath,1),...pick(lib.warm,2)]},{name:"Conditioning",minutes:Math.max(8,Math.round(w*.30)),items:pick(lib.cardio,diff==="easy"?1:2)},{name:"Core durability",minutes:Math.max(8,Math.round(w*.22)),items:pick(lib.core,diff==="hard"?4:3)},{name:"Strength maintenance",minutes:Math.max(8,Math.round(w*.24)),items:pick(lib.strength,diff==="easy"?2:4)},{name:"Stretch + downshift",minutes:Math.max(5,Math.round(w*.12)),items:[...pick(lib.mob,3),...pick(lib.breath,1)]}];const e=$("plan");e.replaceChildren();plan.forEach((b,i)=>{let d=document.createElement("div");d.className="rb-block";d.innerHTML=`<strong>${i+1}. ${b.name} — ${b.minutes} min</strong><p>${b.items.join(" · ")}</p>`;e.append(d)});$("plan-out").textContent=JSON.stringify(plan,null,2)};function tick(){const m=Math.floor(remaining/60),s=remaining%60;$("timer").textContent=`${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`}$("start").onclick=()=>{if(!plan.length)$("build").click();remaining=plan.reduce((s,b)=>s+b.minutes,0)*60;clearInterval(timer);tick();timer=setInterval(()=>{remaining--;tick();if(remaining<=0)clearInterval(timer)},1000)};$("pause").onclick=()=>clearInterval(timer);$("log").onclick=()=>{logs.push({at:new Date().toISOString(),actualMinutes:+$("actual").value||0,effort:+$("effort").value||0,recovery:$("post").value,notes:$("notes").value,plan});$("log-out").textContent=JSON.stringify(logs,null,2)};$("export").onclick=()=>{const t=JSON.stringify({schema:"zzx.rosebud.training-log.v1",logs},null,2),b=new Blob([t],{type:"application/json"}),u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;a.download="rosebud-training-log.json";a.click()};$("build").click();tick();window.RoseBud={version:"0.1.0-alpha-web"}})();