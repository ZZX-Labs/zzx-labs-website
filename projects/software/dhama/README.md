# Dhama

Functional browser adaptation of the PyQt5 meditation/mindfulness application.

Uses Web Audio for generated local soundscapes and tones, Canvas for synchronized visuals, mantra interval cues, session timing, and local presets. It does not fetch external audio.

Deploy directly into `/projects/software/dhama/`.

`logo.png` is intentionally omitted.
