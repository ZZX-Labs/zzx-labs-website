# CalSched

Calendar and scheduler with recurring events, reminders, persistent local storage, JSON import/export, and ICS export.

**Version:** 0.2.0  
**License:** MIT

Deploy directly into `/projects/software/calsched/`.

`logo.png` is intentionally omitted until final artwork is supplied.
