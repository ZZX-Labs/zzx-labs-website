# VLC AlarmClock native scheduler

Run:

```bash
python vlc_alarmclock.py vlc-alarmclock.json --vlc vlc
```

The helper launches only the media paths explicitly contained in the exported configuration. Review the JSON before use.
