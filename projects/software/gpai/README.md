# GP-AI

SFW browser research companion for GP datasets, model configuration, fine-tuning plans, and generation-job manifests.

The page does not execute PyTorch/Transformers training. It produces reproducible research artifacts for native/local pipelines.

Deploy directly into `/projects/software/gpai/`.

`logo.png` is intentionally omitted.
