# VMC hardware profiles

The VMC software page uses a simple serial/event protocol so multiple embedded variants can share one session model.

Planned/known profiles include:
- ESP32 / LilyGO T-Display
- Arduino Nano
- Arduino Micro
- Arduino Mini
- Arduino Uno R4

The browser page does not claim to flash firmware. Use the exported protocol/config as the contract for native/embedded implementations.
