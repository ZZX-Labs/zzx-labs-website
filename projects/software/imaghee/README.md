# ImaGhee

Functional browser image converter/rescaler supporting PNG, JPEG, and WebP export, resize presets, aspect-ratio controls, and metadata-stripping through Canvas re-encoding. TIFF conversion remains in the native Pillow/FFmpeg version.

Deploy directly into `/projects/software/imaghee/`.

`logo.png` is intentionally omitted.
