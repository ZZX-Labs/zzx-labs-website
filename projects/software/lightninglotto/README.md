# LightningLotto

Provable-fair browser simulation for fixed UTC lottery rounds. Uses a SHA-256 server commitment, user-supplied public Bitcoin block hash, normalized simulated entries, deterministic weighted draw, and reproducible verification proof.

The webpage accepts no payments and requests no wallet keys, invoices, macaroons, or Lightning credentials. Settlement remains external/native.

Deploy into `/projects/software/lightninglotto/`.
