# gNee

Declarative browser control-plane planner for sites, services, dependencies, safe operator runbooks, and JSON/YAML configuration. It collects no SSH credentials and performs no remote execution.

Deploy directly into `/projects/software/gnee/`.

`logo.png` is intentionally omitted.
