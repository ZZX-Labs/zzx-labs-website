# FullNode-Scraper

Browser companion for analysis of public Bitcoin node snapshots.

The web page imports/normalizes node datasets, reports Tor/clearnet and country distributions, generates six-hour collection plans, and exports JSON/CSV. It does not scan arbitrary hosts or run nmap from the browser.

Deploy directly into `/projects/software/fullnode-scraper/`.

`logo.png` is intentionally omitted.
