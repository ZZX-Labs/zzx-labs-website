# Hasher

Functional browser adaptation of the recursive file lister/hasher.

Supports file and directory selection, SHA-256/SHA-1, metadata catalogs, duplicate detection, comparison with prior catalogs, and forensic-friendly JSON/CSV exports.

Deploy directly into `/projects/software/hasher/`.

`logo.png` is intentionally omitted.
