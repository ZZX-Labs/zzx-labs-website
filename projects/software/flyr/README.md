# flyr

Functional browser adaptation of the generative flyer/poster/branding engine.

Implements deterministic seed-based tessellations, radial symmetry, fractal branching, hybrid compositions, headline/subhead layout, format presets, PNG export, and reproducible design JSON export.

Deploy directly into `/projects/software/flyr/`.

`logo.png` is intentionally omitted.
