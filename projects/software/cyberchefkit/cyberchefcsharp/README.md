# CyberChefC#

C#/.NET CyberChefKit implementation companion.

**Version:** 0.1.0-alpha  
**License:** CLA Apache

The browser subpage executes reference transforms, generates a .NET API, `.csproj`, CLI entry point, xUnit-style vectors, and a portable integration bundle.

Deploy directly into `/projects/software/cyberchefkit/cyberchefcsharp/`.

`logo.png` is intentionally omitted so existing artwork is not overwritten.

Parent project: `/projects/software/cyberchefkit/`.
