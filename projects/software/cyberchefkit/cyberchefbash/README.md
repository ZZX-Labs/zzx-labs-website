# CyberChefBash

Bash wrapper layer for Unix CyberChefKit pipelines.

Deploy directly into `/projects/software/cyberchefkit/cyberchefbash/`.

Version `0.1.0-alpha`, CLA Apache. The web companion generates allowlisted wrappers, pipeline examples, dependency checks, and portable bundles. `logo.png` is intentionally omitted.
