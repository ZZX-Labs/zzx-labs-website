# GridHub

Functional browser implementation of the live web-thumbnail dashboard.

Supports user-defined URL tiles, grid columns, heights, zoom, fullscreen, refresh intervals, layout presets, and dashboard JSON export. Sites that prohibit iframe embedding cannot be forced into the browser grid.

Deploy directly into `/projects/software/gridhub/`.

`logo.png` is intentionally omitted.
