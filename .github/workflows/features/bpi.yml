name: Disabled Feature - ZZX BPI API

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: printf '%s\n' 'Disabled future feature workflow placeholder.'
